# Dependencies for pto-tracker!
# Install them by executing in this directory:
#
#   brew bundle
#
# Track dependencies with an explanation of what each does.

# handles accounting
brew 'ledger'
# converts stdin to HTML, including preserving colors
brew 'aha'
# SMTP client more easily configured than sendmail
brew 'msmtp'
